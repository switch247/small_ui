import React from "react";
import "../css/elements/elements.css";

interface Props {
  backgroundColor: string;
  color: string;
}
const CurvedButton: React.FC<Props> = ({ backgroundColor, color }) => {
  return <button></button>;
};

export default CurvedButton;
